{
  // to create a migration, run the following command in the terminal at the root of the solution:
  // dotnet ef migrations add MigrationName --project TheBlog.Infra --startup-project TheBlog.API
  // to apply a migration, run the following command in the terminal at the root of the solution:
  // dotnet ef database update --project TheBlog.Infra --startup-project TheBlog.API
  "ConnectionStrings": {
    "SqlServer": ""
  },
  "Settings": {
    "Jwt": {
      "ExpirationTimeInMinutes": 60,
      "SigningKey": "mmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmm"
    }
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
